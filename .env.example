# Model Provider API Keys
OPENROUTER_API_KEY=
GEMINI_API_KEY=
ANTHROPIC_API_KEY=

# Optional Discord Integration
DISCORD_BOT_TOKEN=

# Host directory to mount as /ws in the container (defaults to ./workspace)
WORKSPACE_DIR=./workspace

# Host user and group ID for workspace file permissions (defaults to 1000:1000)
# UID=1000
# GID=1000
